name: plumber_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  country_code_picker_x: ^2.0.5
  flutter:
    sdk: flutter
  geocoding: ^3.0.0
  geolocator: ^13.0.2
  get: ^4.6.6
  google_fonts: ^6.2.1
  google_maps_flutter: ^2.10.0
  permission_handler: ^11.3.1
  pinput: ^5.0.1
  http: ^1.1.0
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/Logos/